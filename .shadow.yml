# Shadow PR review bot config — see https://github.com/sudsali/shadow
codebase:
  src_dir: aws_lambda_powertools     # primary source root
  file_ext: .py                      # narrow tool searches to Python
  test_dir: tests                    # used by find_tests_for
  language: python                   # legacy issue-respond path only

bot:
  name: shadow
  escalate_label: needs-human

models:
  investigator: us.anthropic.claude-opus-4-7
  critic: us.anthropic.claude-opus-4-7
  reporter: us.anthropic.claude-haiku-4-5-20251001-v1:0
